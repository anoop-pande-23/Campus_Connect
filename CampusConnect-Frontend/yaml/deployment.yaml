apiVersion: apps/v1
kind: Deployment
metadata:
  name: campus-connect-frontend
  namespace: campus-connect
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campus-connect-frontend
  template:
    metadata:
      labels:
        app: campus-connect-frontend
    spec:
      containers:
      - name: frontend
        image: vipul710/campus-connect-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: campus-connect-frontend
  namespace: campus-connect
spec:
  selector:
    app: campus-connect-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP